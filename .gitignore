# ============================ Build / Distribution ============================
dist/
build/
stage/
*.dmg
*.pkg
*.app/
dist_*.app/
*.exe
*.dll

# ================================= Python =====================================
__pycache__/
*.py[cod]
*.pyd
*.so
*.egg-info/
.eggs/
pip-wheel-metadata/
*.whl
.wheels/

# =========================== Virtual environments =============================
# Covers .venv, venv, env and suffixed variants (.venv-macos-arm64, .venv-win-amd64, …)
.venv/
.venv*/
venv/
venv*/
env/
env*/

# ========================= Logs / caches / analysis ===========================
*.log
.pytest_cache/
.mypy_cache/
.coverage*
htmlcov/
artifacts/

# =============================== OS / IDE =====================================
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
.idea/
*.code-workspace
/.vscode
# ========================= Secrets / local config ============================
.env

# ===================== Local runtime data (lives in the OS ====================
# app-data dir, e.g. ~/Library/Application Support/AcaSmart — never the repo).
# resources/acasmart_template.db IS tracked; do not add it here.
acasmart.db
acasmart*backup*.db
/backups/

# ===================== Local scratch / throwaway notes ========================
# NOTE: curated docs (CONTEXT.md, CLAUDE.md, docs/**) are intentionally TRACKED.
# These are one-off, machine-local, or AI-scratch files only.
notes.txt
plan.md
GEMINI.md
old_db.py
گزارش_اساتید_*.xlsx
