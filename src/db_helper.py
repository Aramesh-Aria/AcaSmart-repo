# DEPRECATED MODULE — DO NOT USE
# This module has been retired. Import from domain repos under `data/*` instead.
raise RuntimeError(
    "db_helper.py has been removed. Import from domain modules under `data/` "
    "(e.g., `from data.sessions_repo import add_session`) instead."
)
